## 1. Парсер таймстемпа

- [x] 1.1 Реализовать `parseTimestamp(input)` — возвращает секунды (Number) или `null`
- [x] 1.2 Поддержка форматов: `SS.mmm`, `M:SS.mmm`, `H:MM:SS.mmm`, `M:SS`, `H:MM:SS`
- [x] 1.3 Замена запятой на точку в дробной части, `trim()` на входе
- [x] 1.4 Валидация: неотрицательные числа, минуты/секунды строго `< 60`

## 2. CSS-стили

- [x] 2.1 Добавить в `styles.css` стиль `.ytp-jump-btn` (на основе `.ytp-copy-time-btn`, со стрелкой/таргетом)
- [x] 2.2 Добавить стиль `.ytp-jump-input` (моноширинный, поверх control bar, `position: absolute`)
- [x] 2.3 Добавить состояние ошибки `.ytp-jump-input--error` (красная рамка)

## 3. UI-логика

- [x] 3.1 Реализовать `setupJumpControl()` — вставляет кнопку после `.ytp-copy-time-btn`, проверяет Shorts
- [x] 3.2 Реализовать `openJumpInput()` — создаёт/показывает `<input>`, фокусирует, выделяет содержимое
- [x] 3.3 Реализовать `closeJumpInput()` — удаляет/скрывает input, снимает обработчики
- [x] 3.4 На `Enter`: парсить, при валидном — `applyJump(seconds)` и закрыть; при невалидном — `--error` модификатор
- [x] 3.5 На `Esc` или `blur` — закрыть без применения

## 4. Переход на таймстемп

- [x] 4.1 Реализовать `applyJump(seconds)` — `Math.max(0, Math.min(seconds, video.duration))`, затем `video.currentTime = clamped`
- [x] 4.2 Если `video` отсутствует или `duration` не определён — no-op + закрытие поля

## 5. Автоподстановка из буфера

- [x] 5.1 В `openJumpInput()` пытаться прочитать буфер через `navigator.clipboard.readText()`
- [x] 5.2 Если содержимое валидно по `parseTimestamp` — подставить в `value` и выделить
- [x] 5.3 При отказе/недоступности — `catch` и продолжение с пустым полем

## 6. Клавиатурный шорткат

- [x] 6.1 Реализовать `setupJumpKeyboardShortcut()` — `keydown` на `document`, ключ `g`, guard по фокусу
- [x] 6.2 Вызов один раз при инициализации (не на каждой навигации)

## 7. Интеграция с жизненным циклом

- [x] 7.1 Вызов `setupJumpControl()` в инициализации и в `handleNavigation()` после `setupCopyButton()`
- [x] 7.2 Закрывать открытое поле ввода при навигации между видео

## 8. Тесты

- [x] 8.1 Юнит-тесты `parseTimestamp`: 4 валидных формата, мс через запятую, невалидные строки (`"abc"`, `"1:60:00"`, `"-1"`), пустая строка
- [x] 8.2 Юнит-тест clamp в `applyJump`: значения `< 0`, `> duration`, внутри диапазона