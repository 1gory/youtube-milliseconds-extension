## 1. CSS-стили

- [x] 1.1 Добавить в `styles.css` стиль `.ytp-ms-toggle-btn` (на основе `.ytp-copy-time-btn`)
- [x] 1.2 Добавить модификатор `.ytp-ms-toggle-btn--off` (приглушённое состояние)

## 2. Логика кнопки

- [x] 2.1 Реализовать `setupMillisecondsToggleButton()` — создаёт кнопку, вставляет после `.ytp-copy-time-btn` (или после `.ytp-time-display` если кнопки копирования нет), вешает `click`-обработчик
- [x] 2.2 В обработчике клика: читать `showMilliseconds` из `chrome.storage`, инвертировать, писать обратно
- [x] 2.3 Реализовать обновление визуального состояния кнопки (`--off` модификатор) по текущему значению `showMilliseconds`
- [x] 2.4 Проверка на Shorts — пропуск создания кнопки

## 3. Синхронизация со storage

- [x] 3.1 Подписаться на `chrome.storage.onChanged` в `content.js` и обновлять `showMilliseconds` + визуальное состояние кнопки + перерисовку таймера
- [x] 3.2 Убедиться, что popup-чекбокс реагирует на изменения, инициированные content script (через тот же `storage.onChanged`)

## 4. Интеграция с жизненным циклом

- [x] 4.1 Вызвать `setupMillisecondsToggleButton()` при инициализации и в `handleNavigation()` после `setupCopyButton()`
- [x] 4.2 Удалять прежнюю кнопку перед пересозданием (идемпотентность)

## 5. Тесты

- [x] 5.1 Юнит-тест: клик по кнопке инвертирует значение в моке `chrome.storage`
- [x] 5.2 Юнит-тест: изменение `showMilliseconds` извне обновляет визуальное состояние кнопки