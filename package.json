{
  "name": "youtube-milliseconds-extension",
  "version": "1.3.0",
  "description": "Adds millisecond precision to YouTube video timestamps, copy any moment with one click, and track total watch time",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
